<div class="span12">
  <div class="page-header">
    <h1><a href="/" class="logo">Weddingram</a><br><small id="tagline" class="keep"><%= Tagline.random %></small></h1>
  </div>

  <p>Enter some <strong class="caseSensitive hasPopover">case-sensitive</strong> comma-separated phrases to search:</p>

  <%= form_tag ngram_calculator_path, :method => :get, :remote => true, :id => 'ngramForm', :class => "form-search" do %>
    <div class="searchParams">
      <%= search_field_tag :q, params[:q], :maxlength => 10_000 %>
      <span class="smoothing">Smoothing <span class="hasPopover">(?)</span>:</span>
      <%= number_field_tag :s, params[:s] || 1, :min => 0, :max => 5 %>
    </div>
    <div class="submitButtons">
      <%= submit_tag "Search NYT Weddings & Celebrations", :id => "ngramSubmit", :class => "btn btn-info" %>
      <span id="randomSearch" class="btn">Just tell me what to search for!</span>
    </div>
    
    <%= hidden_field_tag :blank, params[:blank] %>
  <% end %>

  <div id="ngramContainer"></div>

  <div id="articleSummaries"></div>
</div>